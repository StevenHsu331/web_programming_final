{
  "name": "final",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install:all": "yarn && cd frontend && yarn && cd ../backend && yarn",
    "frontend": "cd frontend && yarn start",
    "backend": "cd backend && yarn server",
    "start": "cd backend && npm server",
    "build": "react-scripts build",
    "start_react": "react-scripts start"
  },
  "engines": {
    "node": "19.0.0",
    "npm": "8.19.2"
  },
  "license": "MIT",
  "dependencies": {
    "nodemon": "^2.0.20",
    "react-scripts": "5.0.1"
  }
}
